import React from 'react';

const CartPage: React.FC = () => {
  return (
    <div>
      <h1> Cart[SEPET / SİPARİŞ]</h1>
      {/* Sepet içeriği / Sipariş? */}
    </div>
  );
};

export default CartPage;
